LOG_LEVEL=info
OBSIDIAN_VAULT_PATH=/Users/cdowning/workspaces/profiles/personal/obsidian/vaults/memory

# Semantic vector search (optional — falls back to keyword-only if Ollama is unavailable)
# Requires: ollama pull nomic-embed-text
OLLAMA_BASE_URL=http://localhost:11434
EMBEDDING_MODEL=nomic-embed-text
EMBEDDING_DIMS=768
EMBEDDING_BATCH_SIZE=50
